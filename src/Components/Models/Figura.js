class Figura {
    constructor(id, title, image, descripcion) {
        this.id = id
        this.title = title
        this.image = image
        this.descripcion = descripcion
    }
}

export default Figura